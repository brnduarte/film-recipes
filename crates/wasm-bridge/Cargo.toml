[package]
name = "wasm-bridge"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
raw-decode = { path = "../raw-decode" }
recipe-engine = { path = "../recipe-engine" }
wasm-bindgen = "0.2"
