//! wasm-bindgen glue exposing `raw-decode` and `recipe-engine` to JS
//! (`packages/processing-web`). Built out starting Phase 1 (Spike B needs
//! only enough of this to prove decode + preview perf end-to-end).
