[package]
name = "export-render"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
recipe-engine = { path = "../recipe-engine" }
image = { version = "0.25", default-features = false, features = ["jpeg"] }
thiserror = "1"
